import { SvgIconComponent } from '@mui/icons-material';

/**
 * Represents a social media icon with a label, MUI SvgIcon component, and a URL.
 */
export type SocialIcon = {
    label: string;
    Icon: SvgIconComponent;
    url: string;
};
