import { Box, Typography } from '@mui/material';

export const NotAuthorized = () => (
    <Box>
        <Typography variant="h1">Not Authorized</Typography>
        <Typography variant="body1">
            Welcome to the not authorized page!
        </Typography>
    </Box>
);
